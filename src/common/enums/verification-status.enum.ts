export enum VerificationStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  CREATED = 'created',
  STARTED = 'started',
  SUBMITTED = 'submitted',
  RESUBMISSION_REQUESTED = 'resubmission_requested',
}

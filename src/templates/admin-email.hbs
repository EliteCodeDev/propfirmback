<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 20px; background: #f5f5f5; color: #333; }
    .email-container { max-width: 400px; margin: 0 auto; background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 4px 10px rgba(0, 0, 0, 0.06); }
    .banner { width: 100%; height: auto; border-radius: 8px 8px 0 0; margin-bottom: 20px; }
    .title { color: #007bff; font-size: 24px; font-weight: bold; margin-bottom: 20px; text-align: center; }
    .main-message { color: #333; font-size: 16px; line-height: 1.6; margin-bottom: 20px; white-space: pre-line; }
    .footer-text { color: #666; font-size: 12px; margin-top: 20px; text-align: center; }
    .company-name { color: #007bff; font-weight: bold; }
  </style>
</head>
<body>
  <div class="email-container">
    {{#if banner}}
    <img src="{{banner}}" alt="Banner" class="banner">
    {{/if}}
    <div class="title">{{ title }}</div>
    <div class="main-message">
      {{#if userFirstName}}
      Estimado {{userFirstName}},<br><br>
      {{else}}
      Estimado usuario,<br><br>
      {{/if}}
      {{{body}}}
    </div>
    <div class="footer-text">
      © 2024 <span class="company-name">FundingHero</span>. Todos los derechos reservados.
    </div>
  </div>
</body>
</html>